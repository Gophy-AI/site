---
const currentYear = new Date().getFullYear()

const links = {
  product: [
    { label: 'Features', href: '#features' },
    { label: 'Pricing', href: '#pricing' },
    { label: 'How it Works', href: '#how-it-works' },
  ],
  legal: [
    { label: 'Privacy Policy', href: '/privacy' },
    { label: 'Terms of Service', href: '/terms' },
  ],
}
---

<footer class="bg-gray-900 text-gray-400 py-12">
  <div class="max-w-7xl mx-auto px-4">
    <div class="grid md:grid-cols-4 gap-8 mb-8">
      <div class="md:col-span-2">
        <a href="/" class="flex items-center gap-2 mb-4">
          <img src="/icon.png" alt="Sales Copilot" class="w-8 h-8" />
          <span class="font-bold text-xl text-white">Sales Copilot</span>
        </a>
        <p class="text-gray-400 max-w-sm leading-relaxed">
          AI copilot for pre-sales engineers and solutions architects.
          Close more deals with less effort.
        </p>
      </div>

      <div>
        <h4 class="font-semibold text-white mb-4">Product</h4>
        <ul class="space-y-2">
          {links.product.map((link) => (
            <li>
              <a href={link.href} class="hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold text-white mb-4">Legal</h4>
        <ul class="space-y-2">
          {links.legal.map((link) => (
            <li>
              <a href={link.href} class="hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="border-t border-gray-800 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
      <p class="text-sm">
        &copy; {currentYear} Sales Copilot. All rights reserved.
      </p>
      <div class="flex items-center gap-4">
        <span class="text-sm">Works with</span>
        <div class="flex items-center gap-3 text-gray-500">
          <span title="Google Meet">Meet</span>
          <span title="Zoom">Zoom</span>
          <span title="Microsoft Teams">Teams</span>
        </div>
      </div>
    </div>
  </div>
</footer>
