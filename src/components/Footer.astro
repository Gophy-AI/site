---
const currentYear = new Date().getFullYear()

const links = {
  product: [
    { label: 'Features', href: '#features' },
    { label: 'How it Works', href: '#how-it-works' },
    { label: 'Models', href: '/models' },
    { label: 'GitHub', href: 'https://github.com/gophy-ai/desktop', external: true },
    { label: 'Download', href: 'https://github.com/gophy-ai/desktop/releases/latest', external: true },
  ],
  legal: [
    { label: 'Privacy Policy', href: '/privacy' },
    { label: 'Terms of Service', href: '/terms' },
  ],
}
---

<footer class="bg-zinc-950 text-zinc-400 py-12">
  <div class="max-w-7xl mx-auto px-4">
    <div class="grid md:grid-cols-4 gap-8 mb-8">
      <div class="md:col-span-2">
        <a href="/" class="flex items-center gap-2 mb-4">
          <img src="/icon.png" alt="Gophy" class="w-8 h-8" />
          <span class="font-bold text-xl text-white">Gophy</span>
        </a>
        <p class="text-zinc-400 max-w-sm leading-relaxed">
          The meeting copilot that remembers. Context that compounds across every conversation.
        </p>
      </div>

      <div>
        <h4 class="font-semibold text-white mb-4">Product</h4>
        <ul class="space-y-2">
          {links.product.map((link) => (
            <li>
              <a
                href={link.href}
                class="hover:text-white transition-colors"
                {...(link.external ? { target: '_blank', rel: 'noopener noreferrer' } : {})}
              >
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>

      <div>
        <h4 class="font-semibold text-white mb-4">Legal</h4>
        <ul class="space-y-2">
          {links.legal.map((link) => (
            <li>
              <a href={link.href} class="hover:text-white transition-colors">
                {link.label}
              </a>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="border-t border-zinc-800 pt-8 flex flex-col md:flex-row justify-between items-center gap-4">
      <p class="text-sm">
        &copy; {currentYear} Gophy. All rights reserved.
      </p>
      <div class="flex items-center gap-4">
        <span class="text-sm">Native macOS app for Apple Silicon</span>
      </div>
    </div>
  </div>
</footer>
