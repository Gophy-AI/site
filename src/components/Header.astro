---
const navLinks = [
  { href: '#features', label: 'Features' },
  { href: '#how-it-works', label: 'How it Works' },
  { href: '#pricing', label: 'Pricing' },
]
---

<header class="fixed top-0 w-full bg-white/80 backdrop-blur-md border-b border-gray-100 z-50">
  <nav class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center justify-between md:grid md:grid-cols-3 h-16">
      <a href="/" class="flex items-center gap-2 md:justify-self-start">
        <img src="/icon.png" alt="Gophy" class="w-8 h-8 rounded-lg flex-shrink-0" />
        <span class="font-bold text-xl text-gray-900 whitespace-nowrap">Gophy</span>
      </a>

      <div class="hidden md:flex items-center justify-center space-x-8">
        {navLinks.map((link) => (
          <a
            href={link.href}
            class="text-gray-600 hover:text-gray-900 transition-colors"
          >
            {link.label}
          </a>
        ))}
      </div>

      <a
        href="https://chrome.google.com/webstore"
        target="_blank"
        rel="noopener noreferrer"
        class="bg-brand-500 text-white px-4 py-2 rounded-lg font-medium hover:bg-brand-600 transition-colors md:justify-self-end whitespace-nowrap"
      >
        Install Free
      </a>
    </div>
  </nav>
</header>
